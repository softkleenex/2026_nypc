# NYPC Agent Context

이 문서는 기존 루트 `AGENTS.md`와 `CLAUDE.md`를 하나로 묶은 단일 작업 지침이다. 원문 내용은 아래 영역에 그대로 포함했다.

## 읽기 순서

1. 기존 `AGENTS.md` 영역에서 저장소 구조, 기본 명령, 스타일, 데이터 안전 규칙을 확인한다.
2. 기존 `CLAUDE.md` 영역에서 현재 후보, 최근 검증, 절대 규칙, 다음 작업을 확인한다.
3. 실제 전략 판단은 `docs/workflow-strategy.md`와 서버/유저 로그를 함께 본다.

## 보존 원칙

원본 문서의 내용은 축약하지 않았다. 별도 통합 검사 문서는 공개 정리 과정에서 제거했고, 이 파일 자체를 보존본으로 사용한다.

## Source: former `AGENTS.md`

> 통합 대상 원문: Codex/기여자 가이드. 아래 내용은 원본을 그대로 포함한다.

# Repository Guidelines

## Project Structure & Module Organization

`bots/` contains numbered, self-contained Python bot submissions. Keep server and user logs in `bots/<n>_nypc_log/` and `bots/<n>_user_log*/` as source data. `tools/` holds evaluation, replay, and log-analysis scripts; `run_many.py` forwards to `tools/evaluate.py`. `nation-providing/` contains the official `testing-tool.py`, sample bot, and config. `opponents/proxy.py` provides local proxy opponents. `results/` is generated output from evaluations. `docs/`, `game-rule.md`, `strategy-plan.md`, and `CLAUDE.md` document rules, workflow, and current strategy.

## Build, Test, and Development Commands

There is no package build step. Use Python directly:

```bash
python3 -m py_compile bots/<n>.py tools/*.py
```

Checks syntax for a candidate and tool scripts.

```bash
python3 tools/evaluate_pool.py --bot bots/<n>.py --start 1 --count 5 --side both --pools sample --name <n>-sample-smoke
python3 tools/evaluate.py --bot bots/<n>.py --opponent "python3 $PWD/bots/<base>.py" --left-opponent "python3 $PWD/bots/<base>.py" --side both --start 1 --count 10 --timeout 300 --name <n>-vs-<base> --quiet
```

Runs the standard smoke and sparring gates. For one official-tool match, run from `nation-providing/` with `python3 testing-tool.py --seed 42 -l ../results/smoke.log -a "python3 ../bots/<n>.py" -b "python3 sample-code.py P2"`.

## Coding Style & Naming Conventions

Use Python 3 with four-space indentation. Existing code favors `from __future__ import annotations`, stdlib-only dependencies, `pathlib.Path`, `argparse`, dataclasses where useful, `snake_case` functions, `PascalCase` classes, and uppercase constants. Bot files are numbered (`bots/135.py`); create a new candidate by copying the best server bot to the next number and making one focused change. Submitted bots must stay self-contained and avoid network or external API calls.

## Testing Guidelines

There is no unit-test suite. Treat `py_compile`, sample smoke, sparring, replay, and log analysis as the verification loop. Inspect `results/<name>/results.csv` for WA, max turn time, and side-specific regressions. Use `python3 tools/analyze_server_logs.py bots/<n>_nypc_log` and `python3 tools/log_timeline.py <log> --every 40` to validate server-log hypotheses.

## Commit & Pull Request Guidelines

This workspace root has no Git history, so no project commit convention is detectable. If contributing through Git, use concise imperative commits such as `Add candidate 136 pressure gate`. PRs should state the bot number, exact change, commands run, win/loss/draw and WA counts, max ms from `results.csv`, and any server or user logs used. Link the relevant `strategy-plan.md` note when applicable.

## Data Safety

Do not delete or rewrite `*_nypc_log/`, `*_user_log*/`, or `bots/archive/` data. Treat `results/` as disposable only after conclusions are recorded in `strategy-plan.md`.

## Source: former `CLAUDE.md`

> 통합 대상 원문: Claude 프로젝트 컨텍스트. 아래 내용은 원본을 그대로 포함한다.

# NYPC 2026 Code Battle (NEXT NATION) — 프로젝트 컨텍스트

예선 마감: **2026-07-08 22:00 KST**. 최소 상위 20팀 진출. **7/7부터 동결**
(대표 고정, 모험 제출 금지).

## 현재 상태 (2026-07-07, 동결 무시하고 계속 진행 중 — 사용자 지시)

- **7/8 11시, 134 제출 결과(`bots/143_nypc_log`) 반영 후 후보 `bots/135.py`.**
  134 제출 결과는 공식 1~6번 HQ/턴제한 승, 7번 턴제한 승, 8번
  `DRAW TURN_LIMIT`로 125와 같은 실패 패턴. 143의 8번 로그는 125의 8번과
  `TIME`을 제외하면 완전히 동일했다. 원인 격리: 63/77/90은 143 오른쪽
  고정 재생에서 T49 `UPGRADE 0`(HQ)을 내고 T72 replay-WA 분기로 빠지지만,
  91 이후는 전부 T49 `UPGRADE 2`(기지)를 내며 143 draw를 그대로 재현한다.
  직접 원인은 91에 추가됐던 T40~49 early anti-greed 조건
  `(상대기지 >= 내기지+1 and 상대병력 >= 내병력+2)`이 공식 8번에서 HQ L2를
  막은 것. 135는 134에서 이 조기 가지를 제거하고 기존 `상대기지 >= 내기지+2`
  조건만 유지한다. 검증: py_compile/누수/sample16 통과. 143 전체 오른쪽
  고정 재생은 1~7번 기존 승리 유지, 8번만 T49 HQ 업그레이드 후 T72
  replay-WA 분기(`8W/0L`, WA 1). 4프록시 seed1/41 640게임은 134 기준과
  동일(econ `72/1/7`, `73/1/6`; turtle `79/0/1`, `78/0/2`; rush `71/9/0`,
  `73/7/0`; punch `80/0/0`, `80/0/0`). direct 77/90 seed1/41도 134 기준과
  동일(`42/0/38`, `41/1/38`). direct 91 seed1은 134와 동일하게
  기준 91의 1패를 무승부로 올림(`41/0/39`), seed41은 기준과 동일. 유저
  타깃 214567/212981 replay도 134와 동일한 WA 분기. 판정: **134의 실전
  nypc 8번 draw 원인을 직접 제거한 다음 제출 후보.**
- **7/8 10시 이후 재개: `bots/134.py` 후보.** 중간평가용 `132.py`는
  실패 신호로 무시하고, 서버 마지막 제출 흐름인 `91.py`에서 재시작.
  `133.py`는 `hq_race` 중 HQ 저축 hold를 본부 1홉 안전 시 유지하는
  회계 패치였지만 타깃/프록시 대부분 no-op. `134.py`는 더 직접적으로
  T120+ 상대 HQ 레벨이 앞서고 내 본부 1홉에 적이 없으면 `hq_score_race`로
  보고 HQ 즉시구매 패딩/저축/훈련 hold에서 `hq_race`와 같은 급으로 취급한다.
  방어 회군·공격 목표·압박 임계값은 건드리지 않는다. 검증: py_compile,
  누수 스캔, sample16 통과. 4프록시 seed1/41 640게임은 91 기준과 동일
  (econ `72/1/7`, `73/1/6`; turtle `79/0/1`, `78/0/2`; rush `71/9/0`,
  `73/7/0`; punch `80/0/0`, `80/0/0`). direct: 77 seed1/41/81 기준과 동일
  (`42/0/38`, `41/1/38`, `46/5/29`), 90 seed1/41 기준과 동일, 91 direct는
  seed1에서 기준 `41/1/38`이 `41/0/39`로 1패→무승부 개선, seed41/81은
  기준과 동일. 공식 91_nypc 로그 고정 재생은 8승0패(단 91 자체와 같은
  replay-WA 2건). 판정: **로컬 무회귀 + 직접대결 1건 개선 신호. 실전
  개선은 미검증이라 125/132 계열보다 91 기반 안전 후보로 분류**.
- **10시 중간평가 후보(7/8 09:55): `bots/132.py`.** `125.py`는 공식
  8번 서버 로그에서 `DRAW TURN_LIMIT`인 반면, 기존 제출 `91.py`는 같은
  8번에서 `LEFT_WIN TURN_LIMIT`였으므로 125 그대로 제출은 보류. `132.py`는
  `91.py`를 베이스로 두고, 125의 핵심 버그픽스인 "기지 붕괴 중 안전 재건
  reserve 패딩 우회"만 단일 이식했다. 검증: py_compile 통과, 누수 스캔
  통과, sample16 `16W/0L/0D`, 4프록시 seed1 count40 양진영은 91/125 기준과
  동일(econ `72/1/7`, turtle `79/0/1`, rush `71/9/0`, punch `80/0/0`).
  direct 77/90 seed1 count40 양진영은 각각 `42W/0L/38D`. 공식 로그 고정
  재생은 91 자체도 8번에서 replay-WA가 나므로 실제 공식봇 재대전 근거로는
  쓰지 않는다.
- **7/8 추가 탐색(125 이후, 132 선정 전 기록):**
  `127.py`는 `hq_race` 저축을 확장 큐 앞으로 당기는 시도였지만 표적
  `seed58_left`에서 커맨드 no-op. `128.py`는 `desperate` 중 HQ 점수
  캐치업을 강제로 허용해 `seed58_left` 정상 패배를 무승부로 올렸으나
  77/90 direct seed1/41에서 대회귀(`3/0/77 -> 5/14/61`,
  `8/1/71 -> 7/12/61`)라 기각. `129.py`는 `desperate` 목표를 HQ로
  고정했지만 no-op. `130.py`는 `desperate` 자유 병력을 staging 대기
  없이 HQ로 보내도록 했고 4프록시 seed1/41 및 77.py/90.py direct
  seed1/41 승패가 125와 동일(econ `72/1/7`, `73/1/6`; turtle
  `79/0/1`, `78/0/2`; rush `71/9/0`, `73/7/0`; punch `80/0/0`,
  `80/0/0`; direct `3/0/77`, `8/1/71`)이지만 표적 개선도 없어
  **안전 no-op / 제출 근거 부족**. 당시 결론: 125 유지.
- **7/8 추가 도구/문서 감사:** 사용자 첨부 공식 HTML 전문
  `/Users/.../pasted-text-1.txt` 재확인. `game-rule.md`의 결과 블록
  순서, `END`/`FINISH`, `N=0` 섹션 생략, TLE/초읽기, `data.bin`,
  언어/메모리/샘플 AI 표와 불일치 없음. `bots/125.py` 및 126~130
  누수 스캔 결과 절대경로, 로그 ID, `seed`, `replay`, `WA`, debug/Codex/
  Claude 문자열 없음(프로토콜 `print("OK")`, `COMMAND` 출력만 존재).
  Claude CLI는 로그인 없음으로 실행 불가. Codex CLI는 인증은 있으나
  현재 샌드박스/네트워크에서 `in-process app-server client` 초기화와
  provider reachability가 막혀 read-only 감사 실행 불가.
- **신규 후보(7/8 추가, `bots/125.py`): 기지 붕괴 중 안전
  재건의 reserve 패딩 우회.** 기존 기지 건설 큐는 빈 거점 위에 전사가
  있고 300골드가 있어도 항상 `reserve=유지비+30`까지 요구했다. 압박 중
  훈련은 저축을 우회하므로, 기지 0~1개로 경제가 무너진 상태에서 300골드를
  모아도 다음 훈련/이동비가 그 돈을 먹어 재건이 영구 지연될 수 있었다.
  `125.py`는 `unknown_map`, T45+, 내 기지 1개 이하, 해당 거점 2홉 내 적
  없음일 때만 기지 L1 재건을 300골드 즉시구매로 허용한다. **검증:**
  `204345_A` 고정 리플레이가 T189 HQ패에서 T146 replay-WA 분기로 전환
  (T56 `TRAIN` 대신 `UPGRADE 28`). 62/77 user-log replay 전체 새 손실
  없음. sample16 `16W/0L`. 4프록시 seed1/41 640게임은 124 기준과 동일
  (econ `72/1/7`, `73/1/6`; turtle `79/0/1`, `78/0/2`; rush `71/9/0`,
  `73/7/0`; punch `80/0/0`, `80/0/0`). 77.py/90.py proper direct
  seed1/41도 124와 동일(`3/0/77`, `8/1/71`). 판정: **진짜 회계 우선순위
  보정 + 로컬 무회귀 + 이전 미해결 실전 로그 1건 분기. replay-WA라 실전
  개선은 최종 제출로 확인 필요**.
- **7/8 공식 HTML 전문 재감사:** 사용자 첨부 공식 페이지 HTML 전문을
  `game-rule.md`/`nation-providing/testing-tool.py`/`bots/125.py`와 재대조.
  명령 처리 순서, 이동 정지/최단거리 tie-break, 전투 공격횟수/타겟,
  공성-노동-보급, `FINISH`, `N=0` 결과 섹션, 초읽기/TLE, 제출 크기와
  `data.bin`, 샘플 AI/지원 언어 항목은 현재 `game-rule.md`에 반영되어 있음.
  `bots/125.py` 제출 누수 스캔 결과 절대경로, 로그 ID, seed, replay/WA,
  디버그 문자열 없음. 남은 `print`는 프로토콜 `OK`와 `COMMAND` 출력뿐.
- **7/8 추가 no-op 후보 `bots/126.py`: desperate 카운터레이스의 경로상
  차단칸 도착턴 보정.** 기존 `desperate` 경주 계산은 현재 같은 칸에 적이
  있을 때만 +1턴을 더했지만, 실제 이동은 앞으로 지나갈 경로상 적 점유 칸에
  들어간 뒤 다음 날도 멈출 수 있다. `126.py`는 경로상의 알려진 차단칸마다
  1턴 지연을 더한다. **검증:** 대표 리플레이(`218475_B`, `212177_B`,
  `204345_A`, `215364_A`, `216180_A`, `215758_A`) 결과 125와 동일.
  4프록시 seed1/41 640게임과 77.py/90.py proper direct seed1/41도 125와
  동일(econ `72/1/7`, `73/1/6`; turtle `79/0/1`, `78/0/2`; rush
  `71/9/0`, `73/7/0`; punch `80/0/0`, `80/0/0`; direct seed1 `3/0/77`,
  seed41 `8/1/71`). 판정: **규칙 계산 보강 + 로컬 무회귀지만 명령 분기
  증거 없음. 제출 후보를 125에서 올릴 근거는 부족**.
- **7/8 추가 후보 상태:** `122.py`의 광범위 적 최근 이동 압박 투영은
  proxy econ seed1 회귀(`72/1/7 -> 71/1/8`)로 기각. `123.py`의 T140+
  투영은 77/90 direct seed1 회귀(`3/0/77 -> 3/2/75`)로 기각. `124.py`는
  `turn>=140 and n_en_bases<=n_my_bases`로 투영을 좁혀 4프록시 seed1/41,
  77/90 direct seed1/41 무회귀였고 `215364_A`/`216180_A`/`215758_A`
  replay-WA 분기를 만들었지만, 확정 개선은 미검증. `125.py`의 베이스로만
  사용.
- **신규 후보(7/8 추가, 로컬 최신 `bots/114.py`): HQ L1 고착 중 확장저축
  우회 회계 보정.** 기존 HQ 큐는 확장 저축(`saving_for=300`) 중에도
  `_hq_stuck`이면 HQ 즉시구매를 허용했지만, 당장 못 사는 경우에는
  `saving_for`가 계속 300으로 남아 훈련 hold도 300만 잡았다. 즉 "T80+
  HQ L1 고착은 HQ를 우선해야 한다"는 예외가 실제 저축액에는 반영되지
  않는 큐 회계 누락이었다. `114.py`는 `_hq_bypass and _hq_stuck`일 때
  미구매 시 `saving_for=max(saving_for, HQ비용)`으로 승격한다. **검증:**
  py_compile, 77_user_log 전체 sweep(A `16W/0L/1D`, B `3W/0L/0D`),
  sample16 `16W/0L`, 4프록시 seed1/41 640게임(econ `72/1/7`, `73/1/6`;
  turtle `79/0/1`, `78/0/2`; rush `71/9/0`, `73/7/0`; punch `80/0/0`,
  `80/0/0`), 77.py/90.py proper direct seed1/41(`3/0/77`, `6/1/73`) 모두
  `110.py` 기준과 동일. 판정: **진짜 큐 회계 보정 + 로컬 무회귀,
  실전 개선은 미검증(no-op)**.
- **7/8 추가 기각 후보:** ①`release`를 견제/링수비 차출 이후 재계산하는
  `111.py`는 stale-counter 버그처럼 보였지만 turtle seed41이 `78/0/2 ->
  77/0/3`으로 회귀. ②링수비 제외를 빼고 견제 차출 이후 재계산만 남긴
  `112.py`도 같은 turtle 회귀. ③HQ 우선 저축 승격을 `_hq_stuck`뿐 아니라
  `hq_race/need_train_cap`까지 넓힌 `113.py`는 econ seed41이 `73/1/6 ->
  72/2/6`으로 회귀. 모두 원복/미채택.
- **7/8 추가 감사/기각 후보:** 공식 HTML 전문 재대조와 다중 감사(Codex CLI
  read-only + 서브에이전트) 결과, `bots/114.py`의 제출 문자열 누수는 없음
  (`print("OK")`, 커맨드 출력만 존재, 51KB). `game-rule.md`에는 공식
  TLE와 로컬 테스트툴 WA 표기 차이를 추가 기록. 새 후보 중 ①결원 충원
  병력을 즉시 `worker_slots`에 반영하는 `115.py`는 그럴듯한 회계 보정이지만
  `212177_B`가 T82 replay-WA 분기에서 T115 HQ패로 악화되어 기각.
  ②`open_slots` 저축해제 조건을 HQ 즉시 슬롯으로만 좁힌 `116.py`는
  `215364_A` 무승부를 T136 replay-WA로 바꾸는 신호가 있었지만 프록시
  대회귀(econ seed1 `72/1/7 -> 58/10/12`, econ seed41 `73/1/6 -> 64/11/5`,
  turtle/rush/punch도 손실 증가)로 기각. 결론: 원격 노동 슬롯도 훈련을
  정당화하는 기존 댐이 실제로 필요하다.
- **7/8 추가 탐색/기각 후보:** ①`117.py`는 적 비본부 기지 1개 이하 붕괴
  시 165턴부터 HQ 점수 전환을 시도했지만 `215364_A`에서 완전 no-op
  (적 기지가 190턴까지 2개 이상). ②`118.py`는 내 HQ HP가 이기지 않는
  165턴 이후 HQ 목표 전환, ③`119.py`는 `endgame_chip` 자체를 165턴/+2
  병력우위로 앞당김. 둘 다 `215364_A`를 여전히 무승부로 남겼고 119는
  명령은 바꿨지만 HQ 공성에 실패해 채택 근거 없음. ④`120.py`는 훈련 전
  `open_slots` 계산에서 이동 예정 병력을 stationed에서 제외하는 회계
  보정이었으나 77/90 직접대결 seed41이 기준 `6/1/73`에서 `6/3/71`로
  회귀. ⑤`121.py`는 같은 보정을 HQ에만 좁혔지만 동일 회귀라 기각.
  결론: HQ 점수 전환 단순 조기화와 open-slot departing 회계 보정은 반복
  금지.
- **신규 후보(7/8 추가, 로컬 최신 `bots/110.py`): `desperate` 카운터레이스
  도착턴 계산 보정.** 기존 race gate는 모든 병력이 지금 즉시 목표 HQ로
  방향전환할 수 있다고 보고, 같은 구역에 적이 있으면 그날 이동이 멈추는
  규칙도 무시했다. `110.py`는 `109.py` 위에, 이미 이동 중인 우리 병력은
  기존 목적지까지 간 뒤 HQ로 재지시하는 턴수로 계산하고, 현재 같은 칸에
  적이 있으면 1턴 지연을 더한다. **검증:** 합성 트리거에서 `109.py`는
  `desperate=True`, `110.py`는 `False`로 분리됨. 주요 리플레이
  `212177_B`/`215758_A`/`218475_B`/`215364_A`, 77_user_log 전체 sweep
  (A `16W/0L/1D`, B `3W/0L/0D`), sample16 `16W/0L`, 4프록시
  seed1/41 640게임, 77.py/90.py proper direct seed1/41 모두 `109.py` 및
  `108.py` 기준과 동일. 판정: **진짜 규칙/상태 계산 보정 + 로컬 무회귀,
  실전 개선은 미검증(no-op)**.
- **신규 후보(7/8 추가, `bots/109.py`): 적 HQ 관문 웨이브 카운터 회계 보정.**
  기존 `_gate_n`은 적 HQ 인접 구역에 있는 내 전사를 모두 세어, 이미 다른
  목적지로 이동 중이라 곧 관문을 떠날 병력까지 웨이브 크기에 포함했다.
  `109.py`는 관문에 실제로 남아 있거나 이미 적 HQ로 진입 중인 병력만 센다.
  **검증:** 합성 트리거에서 `108.py`의 단독 `MOVE A35 HQ`가 사라짐. 주요
  리플레이 4개, 77_user_log 전체 sweep, sample smoke, 4프록시 seed1/41,
  77.py/90.py proper direct seed1/41 모두 `108.py`와 동일. 판정:
  **상태 회계 보정 + 로컬 무회귀, 실전 개선은 미검증(no-op)**.
- **7/8 추가 감사 메모:** 외부 Codex read-only 감사는 4개 후보를 냈다.
  위 109/110 두 건은 적용·검증 완료. `supply_fund`에 `UPKEEP` 배수를
  곱하는 보정은 과거에 이미 2차 배치 turtle 소폭 하락으로 반복 금지된
  항목이라 미적용. `nation-providing/testing-tool.py`의 전역 Dijkstra 캐시가
  target id만 키로 쓰는 문제는 확인됐지만, 현재 `evaluate.py`/`run_log_replay.py`
  는 매 게임 testing-tool을 별도 프로세스로 실행하므로 봇 제출 성능과 현재
  검증 결과에는 영향 없음. 인프로세스 다중맵 하네스를 만들 때만 수정 대상.
- **신규 후보(7/8 추가, 제출 후보 `bots/108.py`): 초중반 병력열세+압박 중
  하위 기지 레벨업이 HQ 캐치업을 우회하던 큐 우선순위 보정.** 기존 큐는
  HQ 목표 레벨이 남아 있어도, 압박 중이면 HQ 저축은 열지 않으면서 다음
  단계의 기지 레벨업은 즉시구매 가능할 때 실행했다. 이 때문에 초중반
  병력도 밀리는 상태에서 HQ L3 훈련한도/HP 캐치업 자금을 기지 L2/L3가
  먼저 먹을 수 있었다. `108.py`는 이 차단을 `turn < 100`, `enemy >= mine+4`,
  `under_pressure/attacking`, HQ 목표 미달인 경우로 좁혀 적용한다. **검증:**
  `212177_B` 고정 리플레이가 T106 HQ패에서 T82 replay WA 분기로 전환.
  77_user_log 전체 sweep은 A-side 기존 `16W/0L/1D` 유지, B-side
  `2W/1L/0D -> 3W/0L/0D`(단 replay WA는 실제 승리 확정 아님). 4프록시
  seed1/41 640게임은 100.py 기준과 완전 동일(econ `72/1/7`, `73/1/6`;
  turtle `79/0/1`, `78/0/2`; rush `71/9/0`, `73/7/0`; punch `80/0/0`,
  `80/0/0`). 77.py/90.py proper direct seed1/41도 기준과 동일
  (`3/0/77`, `6/1/73`). sample16은 16승0패, 누출 스캔은 프로토콜
  `print` 두 곳만 남음. 판정: **로컬 무회귀 + 남은 실전 손실 리플레이
  1건을 실제로 건드린 제출 후보**.
- **신규 후보군(7/8, 기각/미채택):** 101의 같은 턴 HQ 업그레이드 레벨을
  공격 게이트에 반영, 102의 ring defense 이동성공 후 카운트, 103의
  normal/raid 공격 래치 원인 보존, 104의 이동중 outgoing 병력 open-slot
  제외는 대표 리플레이에서 no-op. 105/106의 종반 chip 중 HQ 관문 조기
  해제는 `215364_A`에서 명령은 바꿨지만 무승부를 깨지 못하고 소수 투입
  손실만 만들 가능성이 있어 미채택. 107의 넓은 HQ우선 기지레벨업 차단은
  `212177_B`와 프록시는 좋았지만 77/90 direct에서 손실이 크게 증가해
  과범위로 기각. 108은 107을 초중반 병력열세 조건으로 좁힌 채택판.
- **신규 후보(7/8 추가, 제출 후보 `bots/100.py`): RIGHT 내부 경로
  tie-break 보정.** 공식 이동 규칙은 최단경로가 여러 개면 실제 구역 번호가
  더 작은 다음 구역을 고른다. 봇은 RIGHT일 때 내부 지역 번호를
  `N-1-real`로 뒤집어 보므로, RIGHT 관점의 동률은 큰 내부 번호를 골라야
  실제 낮은 구역 번호와 일치한다. 기존 `route_steps()`는 LEFT/RIGHT 모두
  작은 내부 번호를 골라 `desperate` 카운터레이스 도착턴을 드물게 잘못
  평가할 수 있었다. 수정: `self.flip`일 때 동률에서 큰 내부 번호 선택.
  **검증:** 99 대비 주요 손실 고정 리플레이 결과 동일(`215758_A`는 T130
  replay WA, `218475_B`는 T101 replay WA, `212177_B`는 여전히 T106 HQ패).
  4프록시 seed1/41 640게임, 77.py/90.py proper direct seed1/41 모두 99와
  완전 동일. sample16도 16승0패. 판정: **공식 규칙 정확성 보강 + 로컬
  no-op/무회귀**. 성능 개선 근거는 아래 99 후보와 동일.
- **신규 후보(7/8 추가, 100의 베이스): T120+ 병력열세 중 별동
  견제 차단.** `outmassed`는 "적 병력 >= 내 병력 + 5"라서 이미
  `under_pressure`/전시경제 신호로 쓰이는데, 별동 견제(`_hq_squad`)는 이
  신호를 무시해 T120+에도 최대 5+2기를 계속 분산시킬 수 있었다.
  `215758_A`에서 이 분산이 T123부터 실제 발동했고, T130 이후 적 10기+
  우위와 맞물려 본대/본부 방어가 무너졌다. 수정: 별동 견제 조건에
  `not (outmassed and self.turn >= 120)` 추가. **격리:** raid 상태 자체를
  끄는 97은 215758 no-op, 별동 견제만 끄는 98은 215758을 바꾸지만
  `218475_B`의 T91 조기 카운터를 막아 회귀. 99는 T120 게이트로
  215758 개선 신호를 유지하면서 218475의 기존 WA 분기를 보존. **검증:**
  4프록시 seed1/41 640게임 기준 91과 완전 동일(econ 72/1/7, 73/1/6;
  turtle 79/0/1, 78/0/2; rush 71/9/0, 73/7/0; punch 80/0/0, 80/0/0).
  77.py/90.py proper direct seed1/41도 기준과 동일(`3/0/77`, `6/1/73`).
  77_user 손실 고정 리플레이 묶음은 LEFT-side 손실 5개에서 91의
  `4W/1L`이 99의 `5W/0L`로 바뀜(추가 전환은 `215758_A`: HQ패 -> T130
  replay WA). RIGHT-side 손실 묶음은 91과 동일(`212177_B`는 여전히 패배).
  판정: **로컬 무회귀 + 실전 손실 1건을 실제로 건드린 제출 후보**. 단,
  replay WA는 실제 승리 확정이 아니므로 서버 유저평가로 최종 판정.
- **신규 후보군(7/8, 기각/미채택):** 92의 본부 주둔/훈련 open-slot
  이동예정 회계 보정은 타깃 리플레이 커맨드 무변화(no-op). 93/94의
  정규공세 래치 해제도 `215758_A`/`212177_B`에서 커맨드 무변화(no-op).
  95의 `invaded` 시 깊은 병력 전원 회군은 215758 패배를 T155->T156으로
  1턴 늦출 뿐 승패 전환 실패. 96의 광범위 `outmassed` 중 raid/harass
  차단은 프록시/직접대결은 무회귀였지만 `218475_B` 고정 리플레이를 기존
  WA 분기에서 T110 HQ패로 되돌려 과범위로 기각. 97은 no-op, 98은 96의
  회귀 원인으로 기각.
- **웹/논문 참고(7/8):** microRTS/RTS 연구는 넓은 전략 스크립트를 유지하고
  적 근처 제한 상태에만 전술 탐색/전투모델을 얹는 접근이 반복된다. 참고:
  Barriga et al. 2017 "Combining Strategic Learning and Tactical Search in
  Real-Time Strategy Games", Uriarte & Ontañón 2016 "Combat Models for RTS
  Games", Richoux 2020 "microPhantom". 현재 봇에 적용 가능한 형태는 broad
  전술 AI가 아니라 이미 존재하는 `_sim_fight()`를 아주 좁게 쓰는 후보다.
  단, 과거 `defend_at`/contested/move 억제류가 직접대결 회귀를 냈으므로,
  다음 시도도 "특정 손실 로그에서 실제 커맨드가 바뀌고 프록시+직접대결
  무회귀"를 통과해야 한다.
- **이번 Codex 추가 후보(7/8 새벽, 현재 `bots/91.py` 반영): 초반
  `allin_alert` 총병력 게이트 보정.** 기존 주석은 "T8까지 적 총병력
  5+는 전병력 러시의 텔"이라고 되어 있었지만, 실제 코드는 적이 상대 HQ에서
  전체 거리의 35% 이상 전진한 병력(`_adv`) 4기 이상일 때만 올인으로 봤다.
  `eval14_user_log/어 나혼자야.txt`는 3턴에 적 5기가 이미 내 HQ 직선로로
  출발하지만, 도착이 너무 빨라 35% 조건이 켜질 때는 이미 T2 기지 구매와
  확장병 이탈이 끝나 있었다. 수정: T8 이내 적 총병력 5기 이상
  (`_early_mass`)도 기존 `_adv` 조건과 함께 `allin_alert`를 점화한다.
  **검증:** `어 나혼자야` 고정 리플레이가 T18 HQ 패배에서 T195 HQ 파괴
  승리로 전환(`91_allin_mass_uhonjaya`, WA 아님). 4프록시 seed1/41
  640게임은 기존 91 기준과 동일(econ 72/1/7, 73/1/6; turtle 79/0/1,
  78/0/2; rush 71/9/0, 73/7/0; punch 80/0/0, 80/0/0). 77.py/90.py
  proper direct seed1/41도 각각 `3/0/77`, `6/1/73` 유지. 추가 실전
  리플레이 묶음에서 `218475_B`는 WA 분기로 바뀌고 `212177_B`는 여전히
  패배. 판정: **주석-구현 불일치 보정 + 유저 초단기 올인 확정 개선 +
  로컬 무회귀 후보**.
- **이번 Codex 추가 후보(7/8 새벽, 현재 `bots/91.py` 반영): `desperate`
  카운터레이스 경주성 게이트.** 기존 지속침공 카운터는
  `inv_streak>=15 && T>=80 && 적기지>=내기지+3`이면 곧바로
  `desperate=True`가 되어 방어 판단(`threatened`)까지 끄고 전군 역러시를
  커밋했다. `218475_B` 고정 리플레이에서 이 가정이 틀려, T80에 본부
  방어병 9기가 상대 본부로 빠지며 T104 사망. 수정: 래치는 유지하되 매턴
  `내 병력의 상대 HQ 최소 도착턴 <= 적 병력의 내 HQ 최소 도착턴 + 1`일
  때만 실제 `desperate`로 인정. 도착턴은 단순 BFS가 아니라 테스트툴과
  같은 가중 최단경로 tie-break를 따라 한 칸씩 진행한 턴수로 계산한다.
  **검증:** 4프록시 seed1/41 640게임 전부 기준
  `91.py`와 동일(econ 72/1/7, 73/1/6; turtle 79/0/1, 78/0/2; rush
  71/9/0, 73/7/0; punch 80/0/0, 80/0/0). 77.py 및 90.py proper direct
  seed1/41은 각각 `3/0/77`, `6/1/73`으로, 이전 안전 기준
  `3/1/76`, `6/3/71` 대비 손실 감소. `218475_B` 고정 리플레이는
  T104 패배가 T110 패배로 늦어졌지만 승패 전환은 못 함. 판정:
  **계산/상호작용 버그 보정 + 로컬 무회귀 + 직접대결 손실 감소, 실전 개선
  후보**. 단, BFS hop 버전과 공식경로 턴수 버전은 로컬 검증 수치와 샘플
  명령 diff가 동일했다. 직접 개선 신호는 "무조건 desperate"를 막은 데서
  나왔고, 경로 턴수 보정은 **규칙 정확성 보강 + 로컬 no-op**으로 분리해
  해석할 것.
- **이번 Codex에서 새로 기각/미채택한 보조 후보(반복 금지):** ①기지 0개
  경제붕괴 상태에서 HQ 저축 hold를 풀어 훈련하게 하는 `econ_broken` 예외는
  `218475_B`에서 T92/T98 `TRAIN`을 추가했지만 이미 T80 역러시로 본부가
  비어 결과가 그대로라 원복. ②`desperate` 게이트 위에 `n_bases==0`+
  병력열세 시 `threatened=True`를 강제하는 `collapse_defense`는
  `218475_B`에서 명령만 몇 턴 지연시키고 T110 패배로 동일, 직접대결 개선도
  `desperate` 단독과 같아 최소변경 원칙으로 제거.
- **이번 Codex 추가 감사(7/8 새벽, 현재 `bots/91.py` 추가 반영):**
  ①소개 노동자 `worker_slots` 보정. `invaded/early_fortress` 또는
  종반 `endgame_chip`으로 비HQ 건물 노동자를 고정하지 않는 분기에서도
  `worker_slots += cap`으로 군대 크기에서 계속 빼던 모순을 제거. 실제
  고정하지 않은 병력은 `army_size`에 포함된다. ②raid 공격 상태 창 보정.
  `raid` 조건은 `110 <= turn < 175`로 제한되어 있었지만 한 번
  `self.attacking=True`가 되면 `invaded` 전까지 영구 유지되어 창 제한이
  사실상 무력했다. `normal_attack`/`raid_attack`을 분리하고 raid로 켜진
  공격은 175턴 이후 정규 공세 조건이 없으면 해제. ③영구 `_desp_latch`가
  새 race-gate를 우회하지 않도록, pin-feeder/harass 차단 조건을 래치가
  아니라 현재 활성 `desperate`로 변경. ④제출 소스 댓글에서 특정 게임번호,
  턴/명령 수, 구버전 비교 같은 실험 출처성 문구를 제거. **검증:** 최종
  누적 후보 기준 4프록시 seed1/41 640게임 전부 기준과 동일(econ 72/1/7,
  73/1/6; turtle 79/0/1, 78/0/2; rush 71/9/0, 73/7/0; punch 80/0/0,
  80/0/0). 77.py/90.py proper direct seed1/41도 각각 `3/0/77`,
  `6/1/73` 유지. sample16은 16승0패. 누출 스캔은 프로토콜 `print` 두 곳만
  남음. 고정 리플레이에서는 `216949_A`, `218475_B`, `214567_A` 등에서
  상대 고정 커맨드를 WA로 깨는 실제 분기가 생기지만, replay WA 승은 최종
  승리 증거가 아니므로 **로컬 무회귀 + 실전 개선 후보**로만 분류.
- **이번 Codex 규칙/문서 감사:** 공식 HTML 전문과 `game-rule.md`/테스트툴
  대조. 결과 블록 끝 `END`는 공식 HTML 전문에도 "각 턴의 결과는 END
  명령어로 마무리"라고 명시되어 있고, `nation-providing/testing-tool.py`도
  실제로 전송한다. `game-rule.md`의 예전 모호성 주석을 이 사실에 맞게 정정.
  적 이동 `moving_target`은 테스트툴 내부 상태와 달리 우리 봇이 정확히
  복원하지 못하지만, 결과 입력에는 상대의 최종 MOVE 목표가 없어서 정확한
  수리는 불가능하다. 추정 휴리스틱은 이번 라운드의 순수 규칙 버그픽스
  범위를 넘으므로 미적용.
- **이번 Codex 추가 감사(7/7 밤, 공식 HTML 전문 기반):** 사용자 첨부
  문제 페이지 HTML을 텍스트로 추출해 `game-rule.md`/테스트툴/`bots/91.py`
  와 대조. 핵심 규칙(아침 `UPGRADE -> MOVE 비용/목표 -> TRAIN 예약`,
  실제 이동 후 훈련생성, 전투/공성 후 노동/보급, `FINISH` 즉시 종료,
  이동비 확정 후 목적지 건물 변화로 환불/중단 없음)은 기존 문서와 코드가
  일치. `game-rule.md`에 남아 있던 로컬 절대경로(`/Volumes/...`)만
  상대경로로 제거. `bots/91.py` 제출 누출 스캔도 통과.
- **현재 `bots/91.py`에 추가로 남긴 후보 1:** T40~49 anti-greed 조건을 아주
  좁게 보강. 기존은 T40~59에 `상대 기지 >= 내 기지 + 2`일 때만 HQ L1
  물량형으로 보고 HQ L2 구매를 막았는데, `214567_A`는 T40에 이미 상대
  HQ L1 + 기지 +1 + 병력 +2라 T45 HQ L2(훈련한도는 안 오르는 600골드)가
  나감. 새 조건은 `t < 50 and 상대기지 >= 내기지+1 and 상대병력 >= 내병력+2`
  를 추가. **검증:** 4프록시 seed1/41 640게임 전부 기존 91 기준과 동일
  (econ 72/1/7, 73/1/6; turtle 79/0/1, 78/0/2; rush 71/9/0, 73/7/0;
  punch 80/0/0, 80/0/0), 90/77 proper direct seed1/41도 각각 기존 기준
  `3/1/76`, `5/3/72`와 동일. 판정: **로그 타깃 명확, 로컬 무회귀,
  안전하지만 실전 미검증**.
- **현재 `bots/91.py`에 추가로 남긴 후보 2:** L3 기지 수리 보완. 규칙상
  레벨3 기지에 `UPGRADE`를 내면 500골드로 만피 수리되지만, 현재 91은
  `b[2] >= base_lv_target`에서 바로 건너뛰어 `BASE_REPAIR_COST`가 죽은
  상수였다. 76 묶음에 들어갔던 73식 후보를 현재 91+T40 위에 재적용.
  **검증:** 4프록시 seed1/41 640게임 전부 기존 기준과 동일, 90/77 proper
  direct seed1/41도 각각 `3/1/76`, `5/3/72`와 동일. 판정: **진짜 규칙
  누락 보완 + 로컬 무회귀, 실전 개선은 미검증**.
- **이번 Codex 추가 후보(7/7 밤, 현재 91.py에 반영):** ①`near_enemy`도
  `invaders`처럼 적 소유 건물의 노동 한도만큼 할인하도록 보정. 기존은 적
  전방기지 노동자까지 본진 근처 압박 병력으로 세어 `under_pressure`/수비
  트리거를 과민하게 만들 수 있었다. ②L5 본부도 `UPGRADE`가 수리인데,
  `hq_siege_soon`은 `hq[2] < 5` 때문에 L5 손상 본부 긴급수리에 발동하지
  않던 점을 보정. ③종반 `endgame_chip`을 경제 큐 이전 HP로 계산해, 같은
  턴 HQ 업그레이드/수리 예정이어도 낮은 HP 기준으로 칩 공격을 켜던 순서
  불일치를 이동 직전 재계산으로 보정. **검증:** 4프록시 seed1/41 640게임
  모두 기준과 승/패/무 동일(econ 72/1/7, 73/1/6; turtle 79/0/1, 78/0/2;
  rush 71/9/0, 73/7/0; punch 80/0/0, 80/0/0), 90/77 proper direct
  seed1/41도 `3/1/76`, `5/3/72`로 동일. 판정: **계산/룰 보정 + 로컬
  무회귀, 실전 개선 미검증**.
- **이번 Codex 추가 후보(7/8 00시, 현재 91.py에 반영):** `near1`과
  `defend_at` 후보 선정도 `invaders`/`near_enemy`와 같은 압박 계산을 쓰도록
  `enemy_pressure_at(origin,k)`로 통일. 기존은 적 소유 전방기지 노동자를
  본부 인접 위협/요격 후보 병력으로 원시 카운트해, 전군회군과 방어지점
  선정이 과민해질 수 있었다. **검증:** 4프록시 seed1/41 640게임은 기준과
  승/패/무 동일(econ 72/1/7, 73/1/6; turtle 79/0/1, 78/0/2; rush 71/9/0,
  73/7/0; punch 80/0/0, 80/0/0). proper direct seed1은 기준 `3/1/76`과
  동일, seed41은 `5/3/72 -> 6/3/71`로 1무가 1승 전환되고 손실 증가는 없음.
  판정: **계산 보정 + 로컬 무회귀 + 직접대결 소폭 개선 신호, 실전 개선은
  아직 미검증**.
- **이번 Codex에서 새로 기각한 후보(반복 금지):** ①미지맵 확장 목표
  `base_target`에 맞춰 미건설 거점 대기병을 10개 이후에도 고정하는 변경은
  econ seed1 `72/1/7 -> 71/1/8` 회귀. ②외곽 `defend_at`보다 본부
  `garrison`을 항상 우선하는 변경은 rush seed1 `71/9/0 -> 70/10/0`
  회귀. ③이를 100턴 이후로 좁힌 late garrison도 프록시는 통과했지만
  90/77 direct에서 패배가 seed1 `1->5`, seed41 `3->7`로 증가해 기각.
  ④업그레이드 당일 늘어난 노동 한도를 노동자 고정에 반영하는 변경은 turtle
  seed1 `79/0/1 -> 79/1/0` 회귀.
- **이번 Codex에서 새로 기각한 후보(반복 금지):** `early_fortress` 중에도
  `defend_at`이 잡히면 본부 회군 대신 공격받는 전방기지로 모으는 변경은
  218475_B 고정 리플레이에서 적 유닛을 죽여 T62 WA 분기를 만들었지만,
  rush 프록시 seed1이 기준 `71/9/0`에서 `68/12/0`으로 즉시 회귀. `not
  rush_alert`로 좁혀도 같은 회귀라 완전 원복.
- **이번 Codex에서 새로 기각한 후보(반복 금지):** ①신규 기지 건설 시
  이동 중 경유 전사를 건설자에서 제외하는 보정은 프록시 640게임은
  통과했지만 90 direct seed1에서 손실이 `1 -> 3`으로 증가해 기각.
  ②`open_slots`를 업그레이드 예정 레벨의 노동 한도로 계산하는 보정도
  프록시는 통과했지만 90 direct seed1/41 모두 손실 증가라 기각. 둘 다
  "규칙상 더 그럴듯한 계산"이어도 direct 미러에서 전술 균형을 망가뜨림.
- **이번 Codex에서 새로 기각한 후보(반복 금지):** `enemy_within(r,k)`까지
  적 건물 노동한도 할인 압박(`enemy_pressure_at`)으로 바꾸는 보정은
  프록시 seed1/41 640게임은 기준과 승패 동일했지만, 77 직접대결에서
  seed1 `3/1/76 -> 10/3/67`, seed41 `6/3/71 -> 11/8/61`로 손실이 크게
  증가해 기각. 원복: `enemy_within`은 원시 적 위치 기준 유지.
- **이번 Codex에서 새로 기각한 후보(반복 금지):** 95~105턴
  turtle/econ 분류 `_tv`에 `적 기지 +2 이상`도 반영하는 변경은
  타깃 `215758_A`에서 커맨드가 전혀 바뀌지 않아 no-op. 기존 조건만으로도
  해당 분류 또는 이후 행동 병목이 이미 결정되는 것으로 보고 원복.
- **이번 Codex 추가 수정(7/7 밤): `bots/91.py` 결과 재생의 HUNGER 순서 버그 수정.**
  테스트툴은 `전투/공성 -> 노동 -> 보급` 순서인데, 기존 봇은 결과 블록의
  모든 `DAMAGE`를 먼저 적용하고 죽은 전사를 제거한 뒤 노동 수입/유지비를
  재계산했다. `HUNGER`는 노동 후 보급 피해라서, 굶어 죽은 노동자도 그날
  노동 수입에는 기여할 수 있고, 식비도 실제로 낸 전사 수만큼만 빠져야 한다.
  수정: `HUNGER` 피해를 별도로 보관하고, 전투 피해/공성/노동/실제 지불
  유지비 계산 후 마지막에 적용. 검증: 4프록시 seed1/41 총 640게임이
  직전 기준과 승패 완전 동일(econ 72/1/7, 73/1/6; turtle 79/0/1, 78/0/2;
  rush 71/9/0, 73/7/0; punch 80/0/0, 80/0/0). 77/90 직접대결도 직전
  `invworkcap` 계열 결과와 동일(seed1 42승0패38무, seed41 41승3패36무).
  판정: **확정 규칙 버그픽스 + 로컬 no-op/무회귀, 실전 개선은 미검증**.
  제출 파일 주석 누출 스캔 통과, 임시 `bots/92.py` 래퍼는 제거됨.
- **이번 Codex 추가 탐색(7/7 밤, 91.py에 현재 반영된 공격적 후보)**:
  `invaders`가 적 전방 기지의 워커까지 침공군으로 세던 계산을 보정. 단순
  78식 “적 건물 주둔 전원 제외”는 직접대결 seed41에서 1무→1패를 만들었고,
  현재는 더 좁게 **적 소유 건물 주둔 수에서 해당 건물 노동 한도만 할인**한다.
  검증: 4프록시 seed1/41 총 640게임은 `91_l3hold` 기준과 승패 완전 동일,
  sample smoke 10승0패 WA0. 77/90 직접대결은 seed1에서 41승1패38무→
  42승0패38무, seed41에서 40승2패38무→41승3패36무. 합산 손실 수는
  유지(81승3패76무→83승3패74무)하지만 seed68 LEFT가 무승부→HQ파괴패로
  악화되는 단일 리스크가 있어 **확정 개선이 아니라 공격적 후보**로 분류.
  추가로 시도한 두 계산형 후보는 기각: ①이동 중/전투중 병력을 heading에서
  제외( rush 회귀 10패), ②적 같은 칸에서 MOVE 발행 금지( econ 2승→무,
  rush 기준 유지지만 순회귀).
- **이번 Codex 세션 결론(7/7 밤)**: 성능 돌파 후보 3개는 모두 기각.
  ①위험 기지 노동자 보충 금지(`enemy_within(r,2)` 결원충원 skip)는
  `rush` 프록시 2차 배치에서 회귀. ②수비 피딩 억제(`defend_at` 적 우세
  시 HQ 집결)는 프록시는 무회귀까지 좁혔지만 77/90 직접대결에서 손실이
  크게 증가해 기각. ③`near2>=4`+총병력열세를 진짜 웨이브로 보는
  `mass_wave`는 목표 리플레이(216949)를 못 바꿔 기각. **모두 91.py에서
  되돌림.** 남은 변경은 제출용 no-op 정리뿐: 죽은 `MAP_PROFILES`/해시 코드
  제거, 상대명·평가번호 주석 제거. py_compile + 4프록시 seed1 스모크 통과.
- **이번 Codex 세션 추가 후보(91.py에 직접 반영됨, 아직 별도 번호 없음)**:
  반응형 L3 캐치업(`need_train_cap`)이 계산된 뒤 anti-greed(`hq_target=1`)
  및 `under_pressure` 훈련 hold에 의해 실제 구매/저축으로 이어지지 않던
  상호작용 버그 수정. `need_train_cap`이면 anti-greed 적용에서 제외하고,
  HQ 저축 예약/hold에서도 `hq_race/_hq_stuck`과 같은 급으로 취급한다.
  검증: 4프록시 640게임(시드1-80, econ/turtle/rush/punch) 승패 기준
  91과 동일, 샘플16 16승0패. 90/77 직접대결은 각 160게임에서
  **81승3패76무**로 기존 91의 3승3패154무 대비 대량 개선(주로 RIGHT
  시작 턴제한 무승부→본부파괴 승리). 단 77 유저 고정 리플레이의 실제
  손실 7건은 대부분 그대로라, 실전 랭킹 개선은 다음 유저평가 확인 필요.
- **최신 후보(제출 대기): `bots/91.py`** = 90 + **HQ레이스 중 패딩 면제**.
  77_user_log의 cuoilen007전(`212981_B.txt`) 재진단: 우리 기지 6개 vs
  상대 3개로 압도했는데도 상대가 HQ L4, 우리는 L3에 머물러 턴제한 HP
  타이브레이크로 패배. 원인: `hq_race`(상대가 HQ레벨에서 앞서 캐치업
  중)가 활성화돼도 `_extra`(HQ 즉시구매 패딩 300) 면제 조건에 `hq_race`가
  안 들어 있어서, 레이스 도중에도 불필요한 여유금 300을 계속 요구해
  캐치업이 늦어짐. `not hq_race`를 면제 조건에 추가. **검증: 4프록시
  320게임이 90.py와 완전 동일(승/패/무/평균턴까지 일치, 프록시는 hq_race
  상황 자체를 안 만들어 무발동 확인) + 샘플16 16-0 + 77.py 직접대결
  160게임(3승3패154무, 그중 154게임이 턴200 도달 — 바로 이 수정이 겨냥한
  턴제한 시나리오가 반복 재현되는데도 무회귀).**
- **91.py 적대적 코드 감사 (7/7, 깨끗한 세션 서브에이전트) → 상호작용 버그
  2건 발견·수정**: ①anti-greed게이트(`hq_target=1` 강제)가 HQ서킷브레이커
  (`_hq_stuck`)보다 뒤에 계산되던 탓에, "우리 HQ가 L1에 갇힘 + 상대도 HQ
  L1 고정 + 기지/물량 열세"가 겹치면 anti-greed가 서킷브레이커를 완전히
  무력화 — 서킷브레이커가 풀려던 바로 그 L1 고착을 anti-greed가 재고정하는
  역설. 수정: `_hq_stuck`을 anti-greed 계산 앞으로 이동, `not _hq_stuck`을
  래치 조건과 적용 조건 양쪽에 추가. ②"대군의 역설" 훈련동결(`cap=0`,
  병력+12 우위 시 HQ저축 위해 훈련 정지)이 `hq[2]<5`만 보고 `hq_target`은
  안 봐서, anti-greed가 `hq_target=1`로 눌러놓은 상태(HQ를 아예 안 살
  상황)에서도 훈련을 계속 멈춰 골드를 놀리는 순손해 가능 — `hq[2]<5`를
  `hq[2]<hq_target`으로 좁혀 해결. **재검증: 4프록시 320게임 승/패/무
  완전 동일(회귀 0) + 평균종료턴만 소폭 단축(4~5턴, 수정이 실제 발동해
  낭비 턴 감소 — 부작용 없는 순개선 신호) + 샘플16 16-0 + 77.py 직접대결
  160게임(3승3패154무, 수정 전과 동일) + 90.py 직접대결 80게임(1승1패78무
  완전미러).** 감사가 잡아낸 그 외 항목(harass 종반 컷오프 부재, `_hq_squad`
  write-only 등)은 경미/기존부터 있던 것으로 판단해 이번엔 손대지 않음.
- **91.py 추가 수정: 견제부대 종반 컷오프.** 감사가 지적한 "견제부대(최대
  5+2=7기)가 턴182+ 구간에도 계속 흩어져 있을 수 있다"(턴제한 판정은
  본부 HP만 보는데, 그 결정적 구간에 병력을 분산시킴 — 77.py부터 있던
  결함을 91의 멀티목표견제가 소폭 악화) 문제를 `self.turn < 182` 조건
  추가로 해결(기존 `attack_at` 강제전환과 동일 임계값). 재검증: 4프록시
  320게임+샘플16 전부 무변화(우리 프록시는 이 구간까지 견제부대가 안
  남음) + 77.py 직접대결 160게임 5승3패152무(수정 전 3승3패154무 대비
  오차범위 내 소폭 개선, 회귀 아님).
- **후보(제출 대기, 91의 베이스): `bots/90.py`** = 77 + HQ서킷브레이커 +
  anti-greed게이트v2 + 관문게이트35 + 반응형L3캐치업 + 멀티목표견제(경량판).
- **76.py는 실전(서버 8맵) 회귀 발견 — 폐기.** 3번 봇에게 79턴 패배(원래
  62.py는 194턴 승리). 원인 격리를 위해 63.py에 6개 수정을 하나씩만 얹은
  버전(77~80)을 개별 제출 검증: **77(worker_slots fix)·78(invaders fix)
  둘 다 서버 8전8승 개별 확인됨.** 72(강제출격 타이밍)·73(기지수리)은
  아직 미검증 — 조합 문제 또는 이 둘 중 하나가 범인일 가능성.
- **대표: `bots/77.py`** (63 + worker_slots fix만, 서버 확인됨). 78도
  안전하나 worker_slots가 매턴 발동하는 핵심 계산이라 77을 채택.
- **평가#26 (77.py, 7/7 15시): 10승8패2무, 1910점(Tier9), 256위/876팀.**
  업셋 2건(스카이+60, freeconferencefood+510) 전부 "상대 HQ L1 고정 +
  순수물량형" 패턴 — 평가#25 AlphaBeta전과 동일 메커니즘으로 **3연속
  실전 확인**. 나머지 6개 패배는 정상 동급/상위 또는 타이밍러시형.
  **추가로 자기 결함 발견**: 올크크전에서 우리 자신의 HQ가 124턴 내내
  L1에 갇힌 채 패배 — `under_pressure`(적 1기만 근처여도 참) 때문에 HQ
  저축 예약 경로가 게임 내내 한 번도 안 열렸던 게 원인. 63의 수정은
  "즉시구매 패딩"만 고쳤을 뿐 이 경로는 그대로였음.
- **⚠️ 중대 발견 (7/7 저녁): 프록시 무회귀 ≠ 실전 안전.** 88.py를 77.py와
  직접대결(80게임)시켰더니 6승19패로 확실히 밀림 — 원인은 "기지
  2차방어분산"(멀티디펜스)이 실제 대등한 상대 앞에서는 주력방어를
  약화시켰기 때문(프록시는 수동적이라 이 트레이드오프가 안 드러남).
  멀티디펜스만 제거하니 82.py처럼 완벽한 미러(1승1패78무)로 복귀 —
  게이트35/HQ서킷브레이커/anti-greed/반응형L3/멀티견제는 전부 무죄
  확인. **교훈: 새 후보는 반드시 직전 대표와 직접대결(80게임+)로도
  검증할 것 — 프록시 대규모 무회귀만으로는 부족하다.**
- (90.py 상세는 위 91.py 항목 참조. 90 자체는 4프록시 656게임+77과의
  직접대결(80게임, 1승1패78무 완전미러)+샘플16 전부 검증 완료했었음.)
- **구 후보(폐기): `bots/88.py`** = 86 + **최상위권(GPT-5.6, 2230점) 실전
  커맨드 리버스엔지니어링**: raw 로그를 turn-by-turn으로 뜯어보니 우리처럼
  한 웨이브 모아서 미는 게 아니라, 훈련을 계속하며 병력을 3개 이상
  동시 목표로 분산 투입해서 우리 기지를 5→0으로 30턴 만에 초토화시킴.
  물량이 아주 여유로울 때(wave*3) 2번째 견제 목표에 소수(2기)를 얹는
  방식으로 흉내냄. 시도1(wave*2, 3기)은 econ에서 소폭 회귀(2배치 모두
  -1승) → 시도2(wave*3, 2기)로 축소해 완전 무회귀 확인. 상세 아래.
- **구 후보: `bots/86.py`** = 77 + ①HQ L1 서킷브레이커 + ②anti-greed
  게이트 v2 + ③기지 2차방어분산 + ④관문 웨이브 게이트 임계값 20→35
  + ⑤반응형 HQ L3 캐치업(물량 8+ 열세 시 훈련한도 조기 확보). 전부 실전
  로그 직접 진단 기반. **④는 이번 세션 유일하게 순수 프록시로 재현+개선
  확인된 항목**: econ 88.75%→90~91.25%, turtle 97.5%→97.5~98.75% (4개
  배치 전부 무승부→승리 전환, rush/punch 완전 동일). 45까지 올려보니
  일부 배치 회귀 — 35가 스위트스팟. **기각된 시도**: target_bases 10→
  13/16(econ 회귀), hq_target 무조건 조기화 45→35(대회귀, econ 70%까지
  하락). econ 스타일 기본 파라미터는 국소최적이지만, 관문 임계값은
  진짜 개선 여지가 있었음. 상세: docs/session-2026-07-07-eval25-followup.md.
- **대표(현재 서버 제출 상태): `bots/63.py`** — 62.py에서 딱 1줄 수정. **62.py는 archive로 이동.**
  62 = `_extra = 300 if _hq_bypass else 0` → 63 = `_extra = 300 if
  (_hq_bypass and not under_pressure) else 0`. 근거: 평가#25 실유저 패배
  2건(AlphaBeta, MATHS) 디버그 추적 결과, 위협 지속 시 확장 저축(300)이
  이미 무력화됐는데도(hold=0) HQ 즉시구매 패딩(+300)만 남아 HQ가 L1
  (훈련한도 1/턴)에 갇혀 서서히 갈리는 구조적 트랩 발견. 위협 중엔 그
  패딩이 보호할 대상(확장)이 이미 안 쓰이므로 패딩을 해제.
  검증: econ 프록시 3개 독립 배치(총 480게임) 중 2개에서 개선(81→88~90%),
  1개는 완전 무변화(no-op) — 손해로 나온 배치 0. rush(160게임)·
  turtle(80)·punch(80)·62 직접대결(80)·샘플8맵(16, 62와 턴수까지 완전
  동일) 전부 무변화/무손실. WA 0, 시간 변화 없음. 60→62→63 누적으로도
  econ 지표 순개선(87.5%→81.25%→88.75~90.6%). 이중 감사 통과(코드감사가
  제기한 "맵7 재검증 필요"는 직접 커맨드 대조로 오탐 확인 — self.mp가
  unknown_map=True로 항상 덮어써져 MAP_PROFILES는 완전 죽은 코드).
- **62 = 60 + 알려진 8맵에서도 `unknown_map` 강제 True.** 기존엔 알려진
  8맵에서 `MAP_PROFILES`(맵별 하드코딩)를 쓰고 범용 로직은 꺼뒀는데,
  일회성 테스트(`61_test.py`, archive 보관)로 반대로 해봤더니 **하드코딩
  없이 범용 로직만으로 8전 8승 0패 0무** (60은 7승1무0패) — 서버 실측
  확정, 로컬 656게임 대조에서 하방 리스크 0(이중 감사 통과). 단, **이 변경은
  유저 랭킹과 무관** — 랜덤맵(유저전) 코드 경로는 60=62로 완전히 동일하고,
  변경은 오직 "알려진 8맵" 분기에만 영향.
- **60의 핵심(51~60 누적, 유저전에 실제로 적용 중)**: 공성 고정 버그
  수정(50) + 관문 웨이브게이트(임계 20) + 견제부대(5기) + 위협모델. 이
  개선들이 **실제 유저 랭킹에 반영됐는지는 아직 미확인** — 최근 유저 평가
  (#19/20, 1820점)는 이보다 훨씬 이전 세대(47) 기준 데이터.
- **평가 #25 도착함 (7/7 10시, `bots/62_user_log`)** — 51~60 개선이 처음
  반영된 결과: **10승 8패 2무, 1980점(Tier 9, +160), 263위/864팀**. 방향은
  맞으나(Tier7→9, LEE(+30) 첫 근접전 승리) top50(~2100+)엔 아직 거리.
  패배 6/8은 동급·상위(정상), 업셋패 2건(AlphaBeta +210 — 초반 확장전 자체
  패배, MATHS +50 — 테크 앞서다 T80~120 물량러시에 뚫림), 아까운 무 1건
  (4321 +100 — 병력·기지 우위인데 상대 HQ레벨 우위로 타이브레이크 무).
  동결 기간이라 대표 반영 불가, 다음 평가까지 기록만.
- 레이팅 궤적: #14 1750 → #15 1780 → #17 1990(Tier9) → #18 1910 → #19/20
  1820(Tier7, 47 기준) → **#25 1980(Tier9, 62 기준)**.
- 손실원 우선순위 (Elo 재검증 결과): 동급 패배(43%) > 하위권 업셋 > 하위
  상대 무승부. 동급 무승부는 무해. 매치메이킹: 높은 레이팅이 LEFT 배정.

## ⚠️ 중대 발견 (7/7 심야): 4개 실전 손실 리플레이 재검증 — 진단≠발동

cuoilen007뿐 아니라 스카이·freeconferencefood·GPT-5.6까지 원본 매치를
그대로 고정 재생(상대측 기록 커맨드 고정, 우리측만 77.py/91.py로 교체)
해서 비교한 결과, **4개 전부 77.py와 91.py의 커맨드가 100% 동일** —
anti-greed게이트v2·멀티목표견제·hq_race패딩면제 중 어느 것도 이 4개
실전 게임에서 실제로 발동하지 않았다(리플레이 신뢰도는 candidate=77.py
결과가 원본 로그와 정확히 일치함으로 확인). **"이 실전 손실들을
고쳤다"는 주장은 철회 — 검증된 건 무해성(회귀 0)뿐, 유효성은 미확인.**
91.py는 여전히 제출할 가치가 있는 후보(안전하고, 다른 게임에서는
트리거가 발동할 가능성은 남아있음)지만, 다음 유저평가에서 이 손실
패턴들이 그대로 재발해도 놀랄 일이 아니다. 상세: 메모리
`nypc-proxy-pool-gate.md` "방법론 대전환 #4".

**후속(같은 밤)**: eval#26 18게임을 디버그 프로브로 넓게 훑어본 결과,
조건 자체(antigreed/hq_race 등)는 다수 게임에서 자주 참이 되지만 실제
커맨드 변화로는 거의 안 이어짐(오이소박이도 동일 패턴 확인) — 단
**올크크전(HQ서킷브레이커의 원래 동기)만은 예외로, 실제로 91.py가
77.py에 없던 UPGRADE 커맨드를 추가 발행함을 직접 확인**(리플레이는
상대측 고정스크립트가 도중 WA로 무효화돼 최종 승패까진 못 봤지만,
"작동은 한다"는 최초의 직접 증거). 즉 91의 6개 추가분 중 실전 발동이
확인된 건 HQ서킷브레이커 하나뿐 — 나머지는 안전하지만 무발동.

## 다음 할 일

1. **현재 로컬 최신은 `bots/114.py`, 서버 마지막 제출은 `bots/91.py`.**
   114는 108의 실전 손실 1건 분기 후보 위에 109/110/114의 규칙형·회계형
   no-op 보정을 누적한 파일이다. 프록시·직접대결·리플레이 검증은 모두
   무회귀지만, 109/110/114 자체는 실전 개선 증거가 약하므로 제출한다면
   "안전한 규칙 보강" 성격으로 볼 것. 제출은 대회 웹 포털 수동 업로드라
   이 세션 도구로는 수행 불가.
2. **cuoilen007류(물량우위+HQ레벨 타이브레이크 패)의 진짜 원인은 아직
   미해결.** 진단: HQ L4 비용(2400골드) 자체 미도달 + `threatened` 한
   턴 깜빡임에 저축(`hold`)이 0으로 리셋되며 증발. 시도했던 수정
   (threatened 2턴 유예 카운터, 폐기된 92.py)은 목표 시나리오도 못
   고치면서 rush 프록시에 진짜 회귀만 남겨 기각. 방어 트리거 완화는
   이 프로젝트에서 반복 실패해온 패턴(워커강제해제·전방보초 2홉과
   동일 계열) — **다음 세션에서도 서두르지 말고 신중히 접근할 것.**
3. 다음 유저 평가 도착 시 이 패턴 재발 여부와 91.py의 실전 효과(특히
   HQ서킷브레이커 — 6개 추가분 중 유일하게 실전 발동이 직접 확인된
   항목, 상세는 "중대 발견" 절 참조) 성적표를 최우선 확인.
4. 새 평가 데이터 저장 시 백업 갱신:
   `cd bots && tar czf archive/logs_backup_<날짜>.tar.gz *_nypc_log *_user_log`

## 절대 규칙 (docs/workflow.md v3.1이 원본)

- **서버 A/B가 유일한 검증.** 로컬은 샘플 스모크·WA·시간 안전망만.
- **[개정 7/7] 알려진 8맵 안전성 기준**: 기존엔 "커맨드 동일성"(맵4=29세대,
  맵7=30세대 리플레이 diff=0)으로 보호했으나, 62에서 이 보호를 의도적으로
  풀고(`unknown_map` 강제 True) 서버 재제출로 직접 재검증하는 방식으로
  전환 — 범용 로직이 하드코딩보다 실측으로 더 강함(8승0패 vs 7승1무)이
  확인됐기 때문. **제출 무제한 + 즉시 로그 반환**이라는 대회 특성상,
  "알려진 8맵도 서버에 직접 실측 가능"이 이제 표준 검증 수단. 유저전
  개선(=`unknown_map` 게이트 뒤 로직)은 이제 알려진 맵에도 항상 적용됨.
- **로그 정책 (경량)**: 전 로그는 archive/logs_backup_*.tar.gz에 백업됨.
  원본은 게이트 필수분(29_nypc_log/4, 30_nypc_log/7, 45_nypc_log)만 유지.
  새 평가는 분석 -> 백업 갱신 -> 원본 삭제 가능. 게이트 파일 3종만 불가침.
- 큰 변경 묶음은 제출 전 **이중 감사** (적대 코드 감사 + 행동 정량 감사,
  깨끗한 세션 서브에이전트).
- 스크립트 편집 후 저장 검증 필수 (assert 실패 시 전체 미저장 주의).

## 주요 도구

```bash
python3 tools/analyze_server_logs.py bots/<n>_nypc_log
python3 tools/log_timeline.py <log> --every 40
python3 tools/run_log_replay.py <log> --replay-side right --candidate <bot>
python3 tools/evaluate_pool.py --bot <bot> --pools sample --start 1 --count 5 --side both
python3 tools/analyze_user_eval.py bots/<n>_user_log  # 유저 로그: 대전ID·상대점수·
  # 업셋패/아까운무/체급승 자동 정리 (요약 텍스트 파일 자동탐지 + 무제.txt 스타일
  # 파싱, 승패로 우리측 LEFT/RIGHT 역추론). 대전ID 중복 경고가 뜨면 파일명 공백
  # 손상 의심 -- 요약에서 빠진 ID로 수동 매칭.
```

게임 규칙: `game-rule.md`. 제출 이력·A/B 원장·유저전 분석: `strategy-plan.md`.
